import React from "react";

class SectionHeaderTitle extends React.Component {
  render() {
    return <div className="section-header-title">{this.props.title}</div>;
  }
}

export default SectionHeaderTitle;
